export interface IPost{
    body: string,
    cover: string,
    createdAt: string,
    id: number,
    isDraft: string,
    tag: string,
    title: string
}

export interface IMenu {
    title: string,
    to: string
}
